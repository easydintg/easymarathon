# 🚨 СРОЧНО: Ошибка деплоя на Vercel

## ❌ Ошибка

```
npm error 404 '@jsr/supabase__supabase-js@^2.49.8' is not in this registry
```

---

## ✅ Решение за 3 шага

### 1️⃣ Открой `package.json`

Должно быть:
```json
"@supabase/supabase-js": "^2.49.8"
```

НЕ должно быть:
```json
"@jsr/supabase__supabase-js": "^2.49.8"
```

Если видишь `@jsr/` - замени на правильную версию выше ☝️

---

### 2️⃣ Git Push

```bash
git add .
git commit -m "Fix: package.json"
git push origin main
```

**ВАЖНО:** Проверь что `git status` показывает `package.json`!

Если не показывает:
- Открой `.gitignore`
- Удали строку `package.json` если есть
- Повтори git add

---

### 3️⃣ Дождись деплоя

Vercel автоматически запустит новый деплой → через 2-3 минуты готово!

---

## 📚 Документация

### Быстрый старт:
📄 **⚡_ЧТО_ДЕЛАТЬ_СЕЙЧАС.md** - читай сейчас!

### Детально:
📄 **КАК_ЗАГРУЗИТЬ_НА_GITHUB.md** - если не получается git push
📄 **🔧_ИСПРАВЛЕНИЕ_ДЕПЛОЯ.md** - детальное решение
📄 **📖_ПОЛНАЯ_ИНСТРУКЦИЯ_ПО_ДЕПЛОЮ.md** - полное руководство

### После деплоя:
📄 **⚡_БЫСТРЫЙ_ЗАПУСК.md** - настройка Telegram бота

---

## 🔍 Диагностика

### Проверка 1: package.json правильный?

```bash
cat package.json | grep supabase
```

Должно быть: `"@supabase/supabase-js"`
НЕ должно быть: `"@jsr/supabase"`

### Проверка 2: package.json на GitHub?

Открой: `https://github.com/ваш-username/ваш-repo/blob/main/package.json`

Должен быть файл с правильным содержимым!

### Проверка 3: Vercel смотрит на правильную ветку?

Vercel → Settings → Git → Production Branch = `main`

---

## 🎯 Визуальная схема

```
Локально:
package.json ✅ (@supabase/supabase-js)
     ↓
   git add .
     ↓
  git commit
     ↓
  git push
     ↓
GitHub:
package.json ✅ (@supabase/supabase-js)
     ↓
   автоматически
     ↓
Vercel:
Новый деплой ✅
     ↓
  Ready! ✅
     ↓
Приложение работает! 🎉
```

---

## ⚠️ Частые ошибки

### Ошибка 1: git status не показывает package.json

**Причина:** В .gitignore есть `package.json`

**Решение:** Удали из .gitignore

### Ошибка 2: На GitHub старый package.json

**Причина:** Не сделал git push или push не прошёл

**Решение:** Повтори git push, проверь логи

### Ошибка 3: Vercel всё равно ошибка

**Причина:** Кеш Vercel

**Решение:**
1. Vercel → Settings → Clear Build Cache
2. Redeploy

---

## 🆘 Быстрая помощь

**Вариант 1:** Автоматическая проверка
```bash
bash check.sh
```

**Вариант 2:** Ручная проверка
1. Открой package.json
2. Найди строку с `supabase`
3. Должно быть `@supabase/supabase-js`
4. НЕ должно быть `@jsr/`

**Вариант 3:** Деплой через CLI
```bash
npm i -g vercel
vercel login
vercel --prod
```

---

## ✅ Итоговый Checklist

Перед тем как просить помощь, проверь:

- [ ] package.json локально содержит `@supabase/supabase-js`
- [ ] .gitignore НЕ игнорирует package.json
- [ ] git add выполнен
- [ ] git commit выполнен
- [ ] git push выполнен
- [ ] На GitHub правильный package.json
- [ ] Vercel запустил новый деплой
- [ ] Прошло 3+ минуты с момента push

---

## 🎉 Готово!

После успешного деплоя:

1. ✅ Копируй URL с Vercel
2. ✅ Читай **⚡_БЫСТРЫЙ_ЗАПУСК.md**
3. ✅ Настраивай Telegram бота
4. ✅ Запускай марафон! 🎄

---

**Нужна помощь? Открой ⚡_ЧТО_ДЕЛАТЬ_СЕЙЧАС.md!**
